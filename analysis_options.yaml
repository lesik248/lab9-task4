include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "android/**"
    - "ios/**"
    - "linux/**"
    - "web/**"

linter:
  rules:
    avoid_print: false
    prefer_const_constructors: true
